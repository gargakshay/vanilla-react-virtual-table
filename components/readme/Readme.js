import React from 'react';

export default function Readme() {
  return (
    <ol>
      <li>Implemented a virtualized table.</li>
      <li>Set the default row count to 500 and the column count to 26.</li>
      <li>
        Implemented infinite scrolling to dynamically load more data as the user
        scrolls.
      </li>
      <li>
        Ensured that the table header remains fixed for a consistent user
        interface.
      </li>
      <li>Bound the table data with a JSON structure.</li>
      <li>
        To view the data, users can click on the "Output" button in the console
        CTA.
      </li>
    </ol>
  );
}
